---
:report_templates:
  :version: 1
  :key:
  - tenant_id
  - name
  - diagnostic_id
  :data:
  - :tenant_id: <%= Tenant.default_tenant.id %>
    :diagnostic_id: <%= Diagnostic.where(slug: Diagnostic::TDA_SLUG).first&.id %>
    :name: TDA Diagnostic Report (en)
    :state: published
    :version: 1
  - :tenant_id: <%= Tenant.default_tenant.id %>
    :diagnostic_id: <%= Diagnostic.where(slug: Diagnostic::ORG_SLUG).first&.id %>
    :name: ORG View Diagnostic Report (en)
    :state: published
    :version: 1
  - :tenant_id: <%= Tenant.default_tenant.id %>
    :diagnostic_id: <%= Diagnostic.where(slug: Diagnostic::TLV_SLUG).first&.id %>
    :name: Team Leader View Diagnostic Report (en)
    :state: published
    :version: 1
  - :tenant_id: <%= Tenant.default_tenant.id %>
    :diagnostic_id: <%= Diagnostic.where(slug: Diagnostic::T360_SLUG).first&.id %>
    :name: Team Leader 360 View Diagnostic Report (en)
    :state: published
    :version: 1

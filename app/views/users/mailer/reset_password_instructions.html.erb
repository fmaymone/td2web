<% 
  render_params = {
    'url' => edit_password_url(@resource, reset_password_token: @token),
    'name' => @resource.name
  }
%>
<%= Liquid::Template.parse('emailtemplate--html--reset_password_instructions'.t).render(render_params).html_safe %>

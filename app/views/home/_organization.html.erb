<div class="card mx-auto organization-card">
  <div class="card-body">
    <h2 class="card-title">
      <%= organization.name %>
      <%= link_to('Show'.t, organization_path(organization), class: 'btn btn-sm btn-info') %>
    </h5>
    <%= render partial: 'organizations/team_diagnostics', locals: { organization: organization } %>
  </div>
</div>
<br/>
